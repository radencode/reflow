//React modules
import PropTypes from 'prop-types';
import React from 'react';

const Options = () => {
	return (
		<div id='configure-options'>
			<div id='configure-options-name'>Attribute Options</div>
		</div>
	);
};

Options.propTypes = {};

export default Options;
